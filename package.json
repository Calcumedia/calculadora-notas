{
  "name": "calcumedia-calculadora-notas",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check cookie-policy.js && node --check calculator-core.mjs && node --check analytics-core.mjs && node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
